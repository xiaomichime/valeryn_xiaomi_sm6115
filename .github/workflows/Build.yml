# GitHub Actions Workflow for building Xiaomi SM6115 Kernel with ReSukiSU + SUSFS
# Repository: xiaomichime/valeryn_xiaomi_sm6115
# 
# This version (v6) includes COMPLETE FIXES:
# - FIXED: Proper SUSFS source file integration (copy files BEFORE patching)
# - FIXED: Correct patch application order per SUSFS documentation
# - FIXED: SUSFS files copied to correct kernel locations
# - FIXED: Kconfig integration for both KernelSU and fs/Kconfig
# - FIXED: Default defconfig set to chime-stock_defconfig
# - FIXED: NON-KPROBE hook preparation for non-GKI kernels
# - FIXED: Proper fs/Makefile and fs/Kconfig updates
# - FIXED: KPM kept enabled (required for non-GKI KernelSU)

name: Build Kernel with ReSukiSU + SUSFS (v6-Fixed)

on:
  push:
    branches:
      - android16
      - android13
      - main
      - master
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - '.gitignore'
  
  pull_request:
    branches:
      - android16
  
  workflow_dispatch:
    inputs:
      device_config:
        description: 'Device defconfig to use'
        required: true
        default: 'chime-stock_defconfig'
        type: choice
        options:
          - chime-stock_defconfig
          - bengal_defconfig
          - bengal-perf_defconfig
          - bengal-lite_defconfig
          - bengal-lite-perf_defconfig
      
      ksu_version:
        description: 'ReSukiSU version/commit (leave empty for latest)'
        required: false
        default: ''
        type: string
      
      enable_susfs:
        description: 'Enable SUSFS features'
        required: true
        default: true
        type: boolean
      
      susfs_spoof_uname:
        description: 'Enable SUSFS uname spoofing'
        required: false
        default: true
        type: boolean
      
      susfs_hide_kernelsu:
        description: 'Hide KernelSU/SUSFS symbols from detection'
        required: false
        default: true
        type: boolean
      
      enable_debug:
        description: 'Enable KSU debug mode'
        required: true
        default: false
        type: boolean
      
      build_variant:
        description: 'Build variant'
        required: true
        default: 'release'
        type: choice
        options:
          - release
          - debug
      
      use_original_ksu:
        description: 'Use original KernelSU instead of ReSukiSU (better SUSFS compat)'
        required: false
        default: false
        type: boolean

env:
  KERNEL_NAME: Valeryn-KSU
  KERNEL_VERSION: "4.19"
  ARCH: arm64
  SUBARCH: arm64

concurrency:
  group: kernel-build-${{ github.ref }}-${{ inputs.device_config || 'chime-stock_defconfig' }}
  cancel-in-progress: true

jobs:
  prepare:
    name: Prepare Build Environment
    runs-on: ubuntu-22.04
    outputs:
      kernel_major: ${{ steps.kernel-info.outputs.kernel_major }}
      kernel_full: ${{ steps.kernel-info.outputs.kernel_full }}
      device_config: ${{ steps.config.outputs.device_config }}
      build_type: ${{ steps.config.outputs.build_type }}
      susfs_branch: ${{ steps.config.outputs.susfs_branch }}
    
    steps:
      - name: Checkout kernel source
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive
      
      - name: Get kernel version info
        id: kernel-info
        run: |
          KERNEL_VERSION=$(grep "^VERSION" Makefile | awk '{print $3}')
          KERNEL_PATCHLEVEL=$(grep "^PATCHLEVEL" Makefile | awk '{print $3}')
          KERNEL_SUBLEVEL=$(grep "^SUBLEVEL" Makefile | awk '{print $3}')
          
          KERNEL_MAJOR="${KERNEL_VERSION}.${KERNEL_PATCHLEVEL}"
          KERNEL_FULL="${KERNEL_VERSION}.${KERNEL_PATCHLEVEL}.${KERNEL_SUBLEVEL}"
          
          echo "kernel_major=$KERNEL_MAJOR" >> $GITHUB_OUTPUT
          echo "kernel_full=$KERNEL_FULL" >> $GITHUB_OUTPUT
          
          echo "=== Kernel Version ==="
          echo "Major: $KERNEL_MAJOR"
          echo "Full: $KERNEL_FULL"
      
      - name: Determine build configuration
        id: config
        run: |
          # Device config - FIXED: default to chime-stock_defconfig
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            DEVICE_CONFIG="${{ inputs.device_config }}"
          else
            DEVICE_CONFIG="chime-stock_defconfig"
          fi
          
          # Build type
          if [ "${{ inputs.build_variant }}" = "debug" ] || [ "${{ inputs.enable_debug }}" = "true" ]; then
            BUILD_TYPE="debug"
          else
            BUILD_TYPE="release"
          fi
          
          # SUSFS branch based on kernel version
          KERNEL_MAJOR="${{ steps.kernel-info.outputs.kernel_major }}"
          case "$KERNEL_MAJOR" in
            "4.9")  SUSFS_BRANCH="kernel-4.9" ;;
            "4.14") SUSFS_BRANCH="kernel-4.14" ;;
            "4.19") SUSFS_BRANCH="kernel-4.19" ;;
            "5.4")  SUSFS_BRANCH="kernel-5.4" ;;
            "5.10") SUSFS_BRANCH="kernel-5.10" ;;
            "5.15") SUSFS_BRANCH="kernel-5.15" ;;
            "6.1")  SUSFS_BRANCH="kernel-6.1" ;;
            *)      SUSFS_BRANCH="kernel-4.19" ;;
          esac
          
          echo "device_config=$DEVICE_CONFIG" >> $GITHUB_OUTPUT
          echo "build_type=$BUILD_TYPE" >> $GITHUB_OUTPUT
          echo "susfs_branch=$SUSFS_BRANCH" >> $GITHUB_OUTPUT
          
          echo "=== Build Configuration ==="
          echo "Device Config: $DEVICE_CONFIG"
          echo "Build Type: $BUILD_TYPE"
          echo "SUSFS Branch: $SUSFS_BRANCH"

  build:
    name: Build Kernel
    needs: prepare
    runs-on: ubuntu-22.04
    timeout-minutes: 180
    
    steps:
      - name: Checkout kernel source
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive
      
      - name: Set up environment variables
        run: |
          echo "DEVICE_CONFIG=${{ needs.prepare.outputs.device_config }}" >> $GITHUB_ENV
          echo "BUILD_TYPE=${{ needs.prepare.outputs.build_type }}" >> $GITHUB_ENV
          echo "KERNEL_MAJOR=${{ needs.prepare.outputs.kernel_major }}" >> $GITHUB_ENV
          echo "KERNEL_FULL_VERSION=${{ needs.prepare.outputs.kernel_full }}" >> $GITHUB_ENV
          echo "SUSFS_BRANCH=${{ needs.prepare.outputs.susfs_branch }}" >> $GITHUB_ENV
          echo "BUILD_DATE=$(date +%Y%m%d-%H%M)" >> $GITHUB_ENV
      
      - name: Cache Clang toolchain
        id: cache-clang
        uses: actions/cache@v4
        with:
          path: ~/toolchains/clang
          key: clang-proton-${{ runner.os }}
          restore-keys: |
            clang-proton-
      
      - name: Install build dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq \
            build-essential \
            bc \
            bison \
            flex \
            libelf-dev \
            libssl-dev \
            libncurses5-dev \
            libncursesw5-dev \
            device-tree-compiler \
            python3 \
            zip \
            unzip \
            cpio \
            openssl \
            git \
            patch \
            ccache
          
          echo "Dependencies installed successfully"
      
      - name: Set up Clang toolchain
        if: steps.cache-clang.outputs.cache-hit != 'true'
        run: |
          echo "=== Setting up Clang toolchain ==="
          
          mkdir -p $HOME/toolchains
          
          # Clone Proton Clang (works well with 4.19 kernels)
          git clone --depth=1 https://github.com/kdrag0n/proton-clang $HOME/toolchains/clang
          
          echo "Clang toolchain downloaded"
          ls -la $HOME/toolchains/clang/bin/ | head -20
      
      - name: Verify Clang toolchain
        run: |
          echo "CLANG_PATH=$HOME/toolchains/clang" >> $GITHUB_ENV
          echo "$HOME/toolchains/clang/bin" >> $GITHUB_PATH
          
          echo "=== Clang version ==="
          $HOME/toolchains/clang/bin/clang --version
          
          # Set up ccache
          echo "CCACHE_DIR=$HOME/.ccache" >> $GITHUB_ENV
          ccache -M 2G
      
      # ===========================================
      # STEP 1: Clone SUSFS FIRST (needed for patches)
      # ===========================================
      - name: Clone SUSFS
        if: ${{ inputs.enable_susfs != false }}
        run: |
          echo "=== Cloning SUSFS ==="
          echo "Branch: ${{ env.SUSFS_BRANCH }}"
          
          # Clone SUSFS repository with the correct branch
          git clone --depth=1 --branch ${{ env.SUSFS_BRANCH }} https://gitlab.com/simonpunk/susfs4ksu.git susfs || {
            echo "WARNING: Failed to clone branch ${{ env.SUSFS_BRANCH }}, trying master..."
            git clone --depth=1 https://gitlab.com/simonpunk/susfs4ksu.git susfs
          }
          
          cd susfs
          echo "SUSFS commit:"
          git log -1 --oneline
          SUSFS_COMMIT=$(git rev-parse --short HEAD)
          echo "SUSFS_COMMIT=$SUSFS_COMMIT" >> $GITHUB_ENV
          cd ..
          
          # List available files for debugging
          echo "=== SUSFS directory structure ==="
          find susfs -type f \( -name "*.c" -o -name "*.h" -o -name "*.patch" -o -name "Kconfig" \) | head -50
          
          echo "=== SUSFS kernel_patches structure ==="
          ls -la susfs/kernel_patches/
          ls -la susfs/kernel_patches/KernelSU/ 2>/dev/null || true
          ls -la susfs/kernel_patches/fs/ 2>/dev/null || true
          ls -la susfs/kernel_patches/include/linux/ 2>/dev/null || true
      
      # ===========================================
      # STEP 2: Clone KernelSU (ReSukiSU or Original)
      # ===========================================
      - name: Clone and integrate KernelSU
        run: |
          echo "=== Integrating KernelSU ==="
          
          # Choose between ReSukiSU and original KernelSU
          if [ "${{ inputs.use_original_ksu }}" = "true" ]; then
            echo "Using original KernelSU (better SUSFS compatibility)..."
            KSU_REPO="https://github.com/tiann/KernelSU"
          else
            echo "Using ReSukiSU..."
            KSU_REPO="https://github.com/ReSukiSU/ReSukiSU"
          fi
          
          # Clone KernelSU
          if [ -n "${{ inputs.ksu_version }}" ]; then
            echo "Cloning at version: ${{ inputs.ksu_version }}"
            git clone --depth=1 $KSU_REPO KernelSU
            cd KernelSU
            git fetch --depth=1 origin ${{ inputs.ksu_version }} || true
            git checkout ${{ inputs.ksu_version }} 2>/dev/null || echo "Using default branch"
            cd ..
          else
            echo "Cloning latest..."
            git clone --depth=1 $KSU_REPO KernelSU
          fi
          
          cd KernelSU
          echo "KernelSU version info:"
          git log -1 --oneline
          KSU_COMMIT=$(git rev-parse --short HEAD)
          echo "KSU_COMMIT=$KSU_COMMIT" >> $GITHUB_ENV
          cd ..
          
          # Determine drivers directory
          if [ -d "common/drivers" ]; then
            DRIVER_DIR="common/drivers"
          elif [ -d "drivers" ]; then
            DRIVER_DIR="drivers"
          else
            echo "ERROR: drivers/ directory not found"
            exit 1
          fi
          
          echo "DRIVER_DIR=$DRIVER_DIR" >> $GITHUB_ENV
          echo "Using drivers directory: $DRIVER_DIR"
          
          # Create symlink to KernelSU kernel module
          KERNELSU_KERNEL_PATH="$(pwd)/KernelSU/kernel"
          rm -f "$DRIVER_DIR/kernelsu"
          ln -sf "$KERNELSU_KERNEL_PATH" "$DRIVER_DIR/kernelsu"
          echo "Created symlink: $DRIVER_DIR/kernelsu -> $KERNELSU_KERNEL_PATH"
          
          # Verify symlink
          ls -la "$DRIVER_DIR/kernelsu"
          
          # Add kernelsu to drivers Makefile
          if ! grep -q "kernelsu" "$DRIVER_DIR/Makefile"; then
            echo "" >> "$DRIVER_DIR/Makefile"
            echo 'obj-$(CONFIG_KSU) += kernelsu/' >> "$DRIVER_DIR/Makefile"
            echo "Added kernelsu to $DRIVER_DIR/Makefile"
          fi
          
          # Add kernelsu Kconfig to drivers Kconfig
          if ! grep -q "kernelsu/Kconfig" "$DRIVER_DIR/Kconfig"; then
            echo "Adding kernelsu Kconfig to $DRIVER_DIR/Kconfig..."
            
            # Backup original
            cp "$DRIVER_DIR/Kconfig" "$DRIVER_DIR/Kconfig.backup"
            
            # Insert before endmenu or append
            if grep -q "^endmenu" "$DRIVER_DIR/Kconfig"; then
              sed -i "/^endmenu/i source \"$DRIVER_DIR/kernelsu/Kconfig\"" "$DRIVER_DIR/Kconfig"
            else
              echo '' >> "$DRIVER_DIR/Kconfig"
              echo "source \"$DRIVER_DIR/kernelsu/Kconfig\"" >> "$DRIVER_DIR/Kconfig"
            fi
            
            # Verify it was added
            if grep -q "kernelsu/Kconfig" "$DRIVER_DIR/Kconfig"; then
              echo "Successfully added kernelsu Kconfig"
            else
              echo "ERROR: Failed to add kernelsu Kconfig, restoring backup"
              cp "$DRIVER_DIR/Kconfig.backup" "$DRIVER_DIR/Kconfig"
              exit 1
            fi
          fi
          
          echo "=== KernelSU integration complete ==="
      
      # ===========================================
      # STEP 3: NON-KPROBE Hook Preparation for Non-GKI
      # ===========================================
      - name: Prepare NON-KPROBE hooks for non-GKI kernel
        run: |
          echo "=== Preparing NON-KPROBE hooks (Required for non-GKI kernels) ==="
          
          # KernelSU requires manual hook implementation on non-GKI kernels
          # Replace #ifdef CONFIG_KPROBES with #if defined(CONFIG_KPROBES) && 0
          
          KSU_KERNEL_DIR=""
          if [ -L "$DRIVER_DIR/kernelsu" ]; then
            KSU_KERNEL_DIR=$(readlink -f "$DRIVER_DIR/kernelsu")
          elif [ -d "$DRIVER_DIR/kernelsu" ]; then
            KSU_KERNEL_DIR="$DRIVER_DIR/kernelsu"
          fi
          
          if [ -n "$KSU_KERNEL_DIR" ] && [ -d "$KSU_KERNEL_DIR" ]; then
            echo "KernelSU kernel directory: $KSU_KERNEL_DIR"
            
            # Find and modify files that use kprobes
            for file in $(find "$KSU_KERNEL_DIR" -name "*.c" -o -name "*.h"); do
              if grep -q "#ifdef CONFIG_KPROBES" "$file" 2>/dev/null; then
                echo "Modifying $file for NON-KPROBE operation..."
                sed -i 's/#ifdef CONFIG_KPROBES/#if defined(CONFIG_KPROBES) \&\& 0/g' "$file"
                sed -i 's/#if defined(CONFIG_KPROBES)/#if defined(CONFIG_KPROBES) \&\& 0/g' "$file"
              fi
            done
            
            echo "NON-KPROBE preparation complete"
          else
            echo "WARNING: Could not find KernelSU kernel directory"
          fi
      
      # ===========================================
      # STEP 4: Copy SUSFS Source Files (BEFORE patching)
      # ===========================================
      - name: Copy SUSFS source files to kernel
        if: ${{ inputs.enable_susfs != false }}
        run: |
          echo "=== Copying SUSFS source files to kernel ==="
          
          # Create directories if they don't exist
          mkdir -p include/linux
          mkdir -p fs
          
          # Copy header files to include/linux/
          echo "Copying header files..."
          if [ -f "susfs/kernel_patches/include/linux/susfs.h" ]; then
            cp susfs/kernel_patches/include/linux/susfs.h include/linux/
            echo "  ✓ susfs.h -> include/linux/"
          fi
          
          if [ -f "susfs/kernel_patches/include/linux/susfs_def.h" ]; then
            cp susfs/kernel_patches/include/linux/susfs_def.h include/linux/
            echo "  ✓ susfs_def.h -> include/linux/"
          fi
          
          if [ -f "susfs/kernel_patches/include/linux/sus_su.h" ]; then
            cp susfs/kernel_patches/include/linux/sus_su.h include/linux/
            echo "  ✓ sus_su.h -> include/linux/"
          fi
          
          # Copy source files to fs/
          echo "Copying source files..."
          if [ -f "susfs/kernel_patches/fs/susfs.c" ]; then
            cp susfs/kernel_patches/fs/susfs.c fs/
            echo "  ✓ susfs.c -> fs/"
          fi
          
          if [ -f "susfs/kernel_patches/fs/sus_su.c" ]; then
            cp susfs/kernel_patches/fs/sus_su.c fs/
            echo "  ✓ sus_su.c -> fs/"
          fi
          
          # Verify files were copied
          echo ""
          echo "=== Verification ==="
          ls -la include/linux/sus*.h 2>/dev/null || echo "WARNING: No SUSFS headers in include/linux/"
          ls -la fs/sus*.c 2>/dev/null || echo "WARNING: No SUSFS sources in fs/"
      
      # ===========================================
      # STEP 5: Apply SUSFS Kernel Patches
      # ===========================================
      - name: Apply SUSFS kernel patches
        if: ${{ inputs.enable_susfs != false }}
        run: |
          echo "=== Applying SUSFS kernel patches ==="
          
          PATCH_DIR="susfs/kernel_patches"
          PATCHES_APPLIED=0
          PATCHES_FAILED=0
          
          apply_patch() {
            local patch_file="$1"
            local patch_name=$(basename "$patch_file")
            local target_dir="$2"
            
            echo ""
            echo "Applying: $patch_name"
            echo "Target directory: $target_dir"
            
            cd "$target_dir" || return 1
            
            # Try different patch levels
            for level in 1 0 2 3; do
              if patch -p${level} --dry-run < "$patch_file" 2>/dev/null; then
                patch -p${level} < "$patch_file"
                echo "  ✓ Applied with patch -p${level}"
                cd - > /dev/null
                return 0
              fi
            done
            
            # Try git apply
            if git apply --check "$patch_file" 2>/dev/null; then
              git apply "$patch_file"
              echo "  ✓ Applied with git apply"
              cd - > /dev/null
              return 0
            fi
            
            echo "  ✗ Could not apply: $patch_name"
            cd - > /dev/null
            return 1
          }
          
          # Step 5a: Apply KernelSU SUSFS patch (to KernelSU directory)
          echo ""
          echo "=== Step 5a: Apply SUSFS patch to KernelSU ==="
          KSU_KERNEL_DIR=""
          if [ -L "$DRIVER_DIR/kernelsu" ]; then
            KSU_KERNEL_DIR=$(readlink -f "$DRIVER_DIR/kernelsu")
          elif [ -d "$DRIVER_DIR/kernelsu" ]; then
            KSU_KERNEL_DIR="$DRIVER_DIR/kernelsu"
          fi
          
          if [ -n "$KSU_KERNEL_DIR" ] && [ -f "$PATCH_DIR/KernelSU/10_enable_susfs_for_ksu.patch" ]; then
            if apply_patch "$PATCH_DIR/KernelSU/10_enable_susfs_for_ksu.patch" "$KSU_KERNEL_DIR"; then
              PATCHES_APPLIED=$((PATCHES_APPLIED + 1))
            else
              PATCHES_FAILED=$((PATCHES_FAILED + 1))
            fi
          fi
          
          # Step 5b: Apply kernel root patch
          echo ""
          echo "=== Step 5b: Apply SUSFS patch to kernel root ==="
          KERNEL_PATCH="$PATCH_DIR/50_add_susfs_in_kernel-${{ env.KERNEL_MAJOR }}.patch"
          
          if [ -f "$KERNEL_PATCH" ]; then
            if apply_patch "$KERNEL_PATCH" "$(pwd)"; then
              PATCHES_APPLIED=$((PATCHES_APPLIED + 1))
            else
              PATCHES_FAILED=$((PATCHES_FAILED + 1))
            fi
          else
            echo "WARNING: Kernel patch not found: $KERNEL_PATCH"
            PATCHES_FAILED=$((PATCHES_FAILED + 1))
          fi
          
          echo ""
          echo "=== Patch Summary ==="
          echo "Applied: $PATCHES_APPLIED"
          echo "Failed: $PATCHES_FAILED"
      
      # ===========================================
      # STEP 6: Add SUSFS to fs/Makefile and fs/Kconfig
      # ===========================================
      - name: Add SUSFS to fs/Makefile and fs/Kconfig
        if: ${{ inputs.enable_susfs != false }}
        run: |
          echo "=== Adding SUSFS to fs/Makefile ==="
          
          # Check if susfs is already in fs/Makefile
          if ! grep -q "susfs" fs/Makefile; then
            echo "" >> fs/Makefile
            echo "# SUSFS support" >> fs/Makefile
            echo "obj-\$(CONFIG_KSU_SUSFS) += susfs.o" >> fs/Makefile
            echo "Added susfs.o to fs/Makefile"
          else
            echo "SUSFS already in fs/Makefile"
          fi
          
          echo ""
          echo "=== Adding SUSFS to fs/Kconfig ==="
          
          # Create SUSFS Kconfig entry for fs/Kconfig
          SUSFS_KCONFIG_BLOCK='
# SUSFS Configuration
config KSU_SUSFS
	bool "Enable SUSFS support for KernelSU"
	depends on KSU
	default y
	help
	  Enable SUSFS (SUS File System) support for better root hiding.
	  This provides enhanced hiding capabilities for KernelSU.

config KSU_SUSFS_HAS_MAGIC_MOUNT
	bool "SUSFS has magic mount"
	depends on KSU_SUSFS
	default y

config KSU_SUSFS_SUS_MOUNT
	bool "SUSFS mount hiding"
	depends on KSU_SUSFS
	default y
	help
	  Hide mounts from detection.

config KSU_SUSFS_SUS_PATH
	bool "SUSFS path hiding"
	depends on KSU_SUSFS
	default y
	help
	  Hide paths from detection.

config KSU_SUSFS_SUS_MAP
	bool "SUSFS map hiding"
	depends on KSU_SUSFS
	default y
	help
	  Hide memory maps from detection.

config KSU_SUSFS_SPOOF_UNAME
	bool "SUSFS uname spoofing"
	depends on KSU_SUSFS
	default y
	help
	  Spoof uname information to hide kernel modifications.

config KSU_SUSFS_ENABLE_LOG
	bool "Enable SUSFS logging"
	depends on KSU_SUSFS
	default n
	help
	  Enable SUSFS debug logging. Not recommended for production.

config KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS
	bool "Hide SUSFS symbols"
	depends on KSU_SUSFS
	default y
	help
	  Hide KernelSU and SUSFS symbols from detection.

config KSU_SUSFS_OPEN_REDIRECT
	bool "SUSFS open redirect"
	depends on KSU_SUSFS
	default y
	help
	  Enable open redirect for hiding files.
'
          
          # Check if SUSFS config already exists
          if ! grep -q "config KSU_SUSFS" fs/Kconfig; then
            # Add before endmenu or at end of file
            if grep -q "^endmenu" fs/Kconfig; then
              sed -i "/^endmenu/i $SUSFS_KCONFIG_BLOCK" fs/Kconfig
            else
              echo "$SUSFS_KCONFIG_BLOCK" >> fs/Kconfig
            fi
            echo "Added SUSFS config to fs/Kconfig"
          else
            echo "SUSFS config already in fs/Kconfig"
          fi
      
      # ===========================================
      # STEP 7: Configure kernel
      # ===========================================
      - name: Configure kernel
        run: |
          echo "=== Configuring kernel ==="
          
          # Clean previous build
          make mrproper || true
          
          # Create output directory
          mkdir -p out
          
          # Apply base defconfig
          echo "Applying defconfig: ${{ env.DEVICE_CONFIG }}"
          make O=out ARCH=arm64 ${{ env.DEVICE_CONFIG }} \
            CC=$HOME/toolchains/clang/bin/clang \
            CLANG_TRIPLE=aarch64-linux-gnu- \
            HOSTCC=$HOME/toolchains/clang/bin/clang
          
          if [ ! -f "out/.config" ]; then
            echo "ERROR: Failed to create .config"
            exit 1
          fi
          
          echo "=== Enabling KSU and required kernel options ==="
          
          # IMPORTANT: Keep KPM enabled (required for non-GKI KernelSU)
          # CONFIG_KPM is already enabled in defconfig - DO NOT DISABLE
          
          # Enable core KSU option
          ./scripts/config --file out/.config --enable KSU 2>/dev/null || \
          echo "CONFIG_KSU=y" >> out/.config
          
          # Enable required kernel options for KernelSU
          # KPROBES should be enabled but we use NON-KPROBE hooks for non-GKI
          ./scripts/config --file out/.config --enable KPROBES 2>/dev/null || echo "CONFIG_KPROBES=y" >> out/.config
          ./scripts/config --file out/.config --enable KALLSYMS 2>/dev/null || echo "CONFIG_KALLSYMS=y" >> out/.config
          ./scripts/config --file out/.config --enable KALLSYMS_ALL 2>/dev/null || echo "CONFIG_KALLSYMS_ALL=y" >> out/.config
          
          # Enable options required for proper kernel module support
          ./scripts/config --file out/.config --enable MODULES 2>/dev/null || true
          ./scripts/config --file out/.config --enable BINFMT_ELF 2>/dev/null || true
          
          # THREAD_INFO_IN_TASK is required for SUSFS on some kernels
          if grep -q "CONFIG_THREAD_INFO_IN_TASK" out/.config; then
            ./scripts/config --file out/.config --enable THREAD_INFO_IN_TASK 2>/dev/null || true
            echo "Enabled THREAD_INFO_IN_TASK"
          fi
          
          # Enable SUSFS options if requested
          if [ "${{ inputs.enable_susfs }}" != "false" ]; then
            echo "=== Enabling SUSFS options ==="
            
            # Core SUSFS option
            ./scripts/config --file out/.config --enable KSU_SUSFS 2>/dev/null || \
            echo "CONFIG_KSU_SUSFS=y" >> out/.config
            
            # SUSFS sub-features
            ./scripts/config --file out/.config --enable KSU_SUSFS_HAS_MAGIC_MOUNT 2>/dev/null || \
            echo "CONFIG_KSU_SUSFS_HAS_MAGIC_MOUNT=y" >> out/.config
            
            ./scripts/config --file out/.config --enable KSU_SUSFS_SUS_MOUNT 2>/dev/null || \
            echo "CONFIG_KSU_SUSFS_SUS_MOUNT=y" >> out/.config
            
            ./scripts/config --file out/.config --enable KSU_SUSFS_SUS_PATH 2>/dev/null || \
            echo "CONFIG_KSU_SUSFS_SUS_PATH=y" >> out/.config
            
            if [ "${{ inputs.susfs_spoof_uname }}" = "true" ] || [ -z "${{ inputs.susfs_spoof_uname }}" ]; then
              ./scripts/config --file out/.config --enable KSU_SUSFS_SPOOF_UNAME 2>/dev/null || \
              echo "CONFIG_KSU_SUSFS_SPOOF_UNAME=y" >> out/.config
            fi
            
            if [ "${{ inputs.susfs_hide_kernelsu }}" = "true" ] || [ -z "${{ inputs.susfs_hide_kernelsu }}" ]; then
              ./scripts/config --file out/.config --enable KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS 2>/dev/null || \
              echo "CONFIG_KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS=y" >> out/.config
            fi
            
            ./scripts/config --file out/.config --enable KSU_SUSFS_SUS_MAP 2>/dev/null || \
            echo "CONFIG_KSU_SUSFS_SUS_MAP=y" >> out/.config
            
            ./scripts/config --file out/.config --enable KSU_SUSFS_OPEN_REDIRECT 2>/dev/null || \
            echo "CONFIG_KSU_SUSFS_OPEN_REDIRECT=y" >> out/.config
            
            # Optional logging (disabled by default for security)
            if [ "${{ inputs.enable_debug }}" = "true" ]; then
              ./scripts/config --file out/.config --enable KSU_SUSFS_ENABLE_LOG 2>/dev/null || \
              echo "CONFIG_KSU_SUSFS_ENABLE_LOG=y" >> out/.config
            fi
          fi
          
          # Enable debug options if requested
          if [ "${{ inputs.enable_debug }}" = "true" ]; then
            ./scripts/config --file out/.config --enable KSU_DEBUG 2>/dev/null || \
            echo "CONFIG_KSU_DEBUG=y" >> out/.config
            ./scripts/config --file out/.config --enable DEBUG_INFO 2>/dev/null || true
          fi
          
          # Update config - resolve dependencies
          echo "Running olddefconfig..."
          make O=out ARCH=arm64 olddefconfig \
            CC=$HOME/toolchains/clang/bin/clang \
            CLANG_TRIPLE=aarch64-linux-gnu- \
            HOSTCC=$HOME/toolchains/clang/bin/clang
          
          # Verify configuration
          echo "=== Verifying kernel config ==="
          echo "--- KSU Options ---"
          grep -E "CONFIG_KSU|CONFIG_KPM" out/.config || echo "WARNING: No KSU/KPM config found!"
          echo "--- Required Options ---"
          grep -E "CONFIG_KPROBES|CONFIG_KALLSYMS|CONFIG_THREAD_INFO_IN_TASK" out/.config || true
          
          if [ "${{ inputs.enable_susfs }}" != "false" ]; then
            echo "--- SUSFS Options ---"
            grep -E "CONFIG_KSU_SUSFS" out/.config || echo "WARNING: No SUSFS config found!"
          fi
          
          echo "Kernel configuration complete"
      
      # ===========================================
      # STEP 8: Verify SUSFS integration
      # ===========================================
      - name: Verify SUSFS integration
        if: ${{ inputs.enable_susfs != false }}
        run: |
          echo "=== Verifying SUSFS Integration ==="
          
          ERRORS=0
          
          # Check for susfs.h in kernel includes
          if [ -f "include/linux/susfs.h" ]; then
            echo "✓ susfs.h found in include/linux/"
          else
            echo "✗ susfs.h NOT found in include/linux/"
            ERRORS=$((ERRORS + 1))
          fi
          
          # Check for susfs_def.h
          if [ -f "include/linux/susfs_def.h" ]; then
            echo "✓ susfs_def.h found in include/linux/"
          else
            echo "✗ susfs_def.h NOT found in include/linux/"
            ERRORS=$((ERRORS + 1))
          fi
          
          # Check for susfs.c in fs/
          if [ -f "fs/susfs.c" ]; then
            echo "✓ susfs.c found in fs/"
          else
            echo "✗ susfs.c NOT found in fs/"
            ERRORS=$((ERRORS + 1))
          fi
          
          # Check SUSFS in fs/Makefile
          if grep -q "susfs" fs/Makefile; then
            echo "✓ SUSFS found in fs/Makefile"
          else
            echo "✗ SUSFS NOT found in fs/Makefile"
            ERRORS=$((ERRORS + 1))
          fi
          
          # Check SUSFS in fs/Kconfig
          if grep -q "config KSU_SUSFS" fs/Kconfig; then
            echo "✓ SUSFS found in fs/Kconfig"
          else
            echo "✗ SUSFS NOT found in fs/Kconfig"
            ERRORS=$((ERRORS + 1))
          fi
          
          # Check SUSFS config
          if grep -q "CONFIG_KSU_SUSFS=y" out/.config; then
            echo "✓ KSU_SUSFS enabled in kernel config"
          else
            echo "✗ KSU_SUSFS NOT enabled in kernel config"
            ERRORS=$((ERRORS + 1))
          fi
          
          # Check KPM is still enabled (required for non-GKI)
          if grep -q "CONFIG_KPM=y" out/.config; then
            echo "✓ KPM enabled (required for non-GKI KernelSU)"
          else
            echo "⚠ KPM not found in config - this may cause issues on non-GKI"
          fi
          
          if [ $ERRORS -gt 0 ]; then
            echo ""
            echo "WARNING: $ERRORS SUSFS integration issues detected"
            echo "SUSFS may not work correctly. Continuing build anyway..."
          else
            echo ""
            echo "All SUSFS checks passed!"
          fi
      
      # ===========================================
      # STEP 9: Build kernel
      # ===========================================
      - name: Build kernel
        run: |
          echo "=== Building kernel ==="
          
          JOBS=$(nproc --all)
          echo "Building with $JOBS jobs"
          
          # Build kernel with detailed logging
          make -j$JOBS O=out ARCH=arm64 \
            CC="$HOME/toolchains/clang/bin/clang" \
            CLANG_TRIPLE=aarch64-linux-gnu- \
            HOSTCC="$HOME/toolchains/clang/bin/clang" \
            HOSTLD="$HOME/toolchains/clang/bin/ld.lld" \
            2>&1 | tee build.log
          
          BUILD_EXIT_CODE=${PIPESTATUS[0]}
          echo "Build exit code: $BUILD_EXIT_CODE"
          
          # Check if build was successful
          if [ ! -f "out/arch/arm64/boot/Image" ]; then
            echo "ERROR: Kernel Image not found!"
            echo "=== Last 200 lines of build log ==="
            tail -200 build.log
            echo "=== Checking for SUSFS/KernelSU errors ==="
            grep -i "susfs\|kernelsu\|error:" build.log | tail -50
            exit 1
          fi
          
          # Copy kernel images
          if [ -f "out/arch/arm64/boot/Image.gz-dtb" ]; then
            echo "Found Image.gz-dtb"
            cp out/arch/arm64/boot/Image.gz-dtb ${{ env.KERNEL_NAME }}-Image.gz-dtb
          elif [ -f "out/arch/arm64/boot/Image.gz" ]; then
            echo "Found Image.gz"
            cp out/arch/arm64/boot/Image.gz ${{ env.KERNEL_NAME }}-Image.gz
          fi
          
          cp out/arch/arm64/boot/Image ${{ env.KERNEL_NAME }}-Image
          
          echo "=== Build artifacts ==="
          ls -la ${{ env.KERNEL_NAME }}-*
          
          # Show KSU/SUSFS symbols in kernel (if available)
          echo "=== Checking for KSU/SUSFS symbols ==="
          if [ -f "out/System.map" ]; then
            grep -i "ksu\|susfs" out/System.map | head -20 || echo "No KSU/SUSFS symbols found in System.map"
          elif [ -f "out/Module.symvers" ]; then
            grep -i "ksu\|susfs" out/Module.symvers | head -20 || echo "No KSU/SUSFS symbols in Module.symvers"
          fi
          
          echo "Build completed successfully"
      
      # ===========================================
      # STEP 10: Package kernel
      # ===========================================
      - name: Package kernel
        run: |
          echo "=== Packaging kernel ==="
          
          mkdir -p output
          
          # Copy kernel images
          cp ${{ env.KERNEL_NAME }}-Image output/
          [ -f "${{ env.KERNEL_NAME }}-Image.gz" ] && cp ${{ env.KERNEL_NAME }}-Image.gz output/
          [ -f "${{ env.KERNEL_NAME }}-Image.gz-dtb" ] && cp ${{ env.KERNEL_NAME }}-Image.gz-dtb output/
          
          # Copy modules if available
          [ -d "out/modules" ] && cp -r out/modules output/
          
          # Generate version info
          cat > output/version.txt << EOF
          Kernel: ${{ env.KERNEL_NAME }}
          Version: ${{ env.KERNEL_FULL_VERSION }}
          Device Config: ${{ env.DEVICE_CONFIG }}
          Build Date: ${{ env.BUILD_DATE }}
          Build Type: ${{ env.BUILD_TYPE }}
          KernelSU Commit: ${{ env.KSU_COMMIT }}
          SUSFS Commit: ${{ env.SUSFS_COMMIT }}
          SUSFS Branch: ${{ env.SUSFS_BRANCH }}
          SUSFS Enabled: ${{ inputs.enable_susfs || 'true' }}
          SUSFS Uname Spoof: ${{ inputs.susfs_spoof_uname || 'true' }}
          SUSFS Hide Symbols: ${{ inputs.susfs_hide_kernelsu || 'true' }}
          KPM Enabled: YES (Required for non-GKI)
          GitHub Run: ${{ github.run_number }}
          Source Commit: ${{ github.sha }}
          EOF
          
          # Create zip
          cd output
          zip -r ../${{ env.KERNEL_NAME }}-${{ env.BUILD_DATE }}.zip *
          cd ..
          
          # Calculate checksums
          sha256sum ${{ env.KERNEL_NAME }}-${{ env.BUILD_DATE }}.zip > ${{ env.KERNEL_NAME }}-${{ env.BUILD_DATE }}.zip.sha256
          
          echo "Package created: ${{ env.KERNEL_NAME }}-${{ env.BUILD_DATE }}.zip"
          ls -la ${{ env.KERNEL_NAME }}-*
      
      - name: Upload kernel artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.KERNEL_NAME }}-${{ env.BUILD_DATE }}
          path: |
            output/
            ${{ env.KERNEL_NAME }}-${{ env.BUILD_DATE }}.zip
            ${{ env.KERNEL_NAME }}-${{ env.BUILD_DATE }}.zip.sha256
          retention-days: 30
      
      - name: Upload build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-log-${{ env.BUILD_DATE }}
          path: build.log
          retention-days: 7
      
      - name: Upload kernel config
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: kernel-config-${{ env.BUILD_DATE }}
          path: out/.config
          retention-days: 7

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-22.04
    if: startsWith(github.ref, 'refs/tags/')
    
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
      
      - name: Create release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            artifacts/**/*.zip
            artifacts/**/*.sha256
          generate_release_notes: true
          draft: false
          prerelease: ${{ contains(github.ref, '-rc') || contains(github.ref, '-beta') || contains(github.ref, '-alpha') }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
