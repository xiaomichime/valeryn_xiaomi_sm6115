# GitHub Actions Workflow for building Xiaomi SM6115 Kernel with ReSukiSU + SUSFS
# Repository: xiaomichime/valeryn_xiaomi_sm6115
# ReSukiSU: https://github.com/ReSukiSU/ReSukiSU
# SUSFS: https://gitlab.com/simonpunk/susfs4ksu
#
# This version (v4) includes:
# - SUSFS kernel patching for Non-GKI kernels
# - Proper ReSukiSU integration
# - All required kernel config options
# - Better error handling

name: Build Kernel with ReSukiSU + SUSFS

on:
  push:
    branches:
      - android16
      - main
      - master
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - '.gitignore'
  
  pull_request:
    branches:
      - android16
  
  workflow_dispatch:
    inputs:
      device_config:
        description: 'Device defconfig to use'
        required: true
        default: 'chime-stock_defconfig'
        type: choice
        options:
          - bengal_defconfig
          - bengal-perf_defconfig
          - bengal-lite_defconfig
          - bengal-lite-perf_defconfig
          - chime-stock_defconfig
      
      ksu_version:
        description: 'ReSukiSU version/commit (leave empty for latest)'
        required: false
        default: ''
        type: string
      
      enable_susfs:
        description: 'Enable SUSFS features'
        required: true
        default: true
        type: boolean
      
      enable_debug:
        description: 'Enable KSU debug mode'
        required: true
        default: false
        type: boolean
      
      build_variant:
        description: 'Build variant'
        required: true
        default: 'release'
        type: choice
        options:
          - release
          - debug

env:
  KERNEL_NAME: Valeryn-KSU
  KERNEL_VERSION: "4.19"
  ARCH: arm64
  SUBARCH: arm64

concurrency:
  group: kernel-build-${{ github.ref }}-${{ inputs.device_config || 'bengal_defconfig' }}
  cancel-in-progress: true

jobs:
  build:
    name: Build Kernel (${{ inputs.device_config || 'bengal_defconfig' }})
    runs-on: ubuntu-22.04
    timeout-minutes: 180
    
    steps:
      - name: Checkout kernel source
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive
      
      - name: Set up environment
        run: |
          # Determine device config
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "DEVICE_CONFIG=${{ inputs.device_config }}" >> $GITHUB_ENV
          else
            echo "DEVICE_CONFIG=bengal_defconfig" >> $GITHUB_ENV
          fi
          
          # Set build variant
          if [ "${{ inputs.build_variant }}" = "debug" ] || [ "${{ inputs.enable_debug }}" = "true" ]; then
            echo "BUILD_TYPE=debug" >> $GITHUB_ENV
          else
            echo "BUILD_TYPE=release" >> $GITHUB_ENV
          fi
          
          # Get kernel version from Makefile
          KERNEL_VERSION=$(grep "^VERSION" Makefile | awk '{print $3}')
          KERNEL_PATCHLEVEL=$(grep "^PATCHLEVEL" Makefile | awk '{print $3}')
          KERNEL_SUBLEVEL=$(grep "^SUBLEVEL" Makefile | awk '{print $3}')
          echo "KERNEL_FULL_VERSION=${KERNEL_VERSION}.${KERNEL_PATCHLEVEL}.${KERNEL_SUBLEVEL}" >> $GITHUB_ENV
          echo "KERNEL_MAJOR=${KERNEL_VERSION}.${KERNEL_PATCHLEVEL}" >> $GITHUB_ENV
          
          # Generate build timestamp
          echo "BUILD_DATE=$(date +%Y%m%d-%H%M)" >> $GITHUB_ENV
          
          echo "=== Build Environment ==="
          echo "Kernel Version: ${KERNEL_VERSION}.${KERNEL_PATCHLEVEL}.${KERNEL_SUBLEVEL}"
          echo "Device Config: $DEVICE_CONFIG"
          echo "Build Type: $BUILD_TYPE"
      
      - name: Install build dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq \
            build-essential \
            bc \
            bison \
            flex \
            libelf-dev \
            libssl-dev \
            libncurses5-dev \
            libncursesw5-dev \
            device-tree-compiler \
            python3 \
            zip \
            unzip \
            cpio \
            openssl \
            git \
            patch
          
          echo "Dependencies installed successfully"
      
      - name: Set up Clang toolchain
        run: |
          echo "=== Setting up Clang toolchain ==="
          
          mkdir -p $HOME/toolchains
          
          # Clone Proton Clang (works well with 4.19 kernels)
          git clone --depth=1 https://github.com/kdrag0n/proton-clang $HOME/toolchains/clang
          
          echo "Clang toolchain ready"
          ls -la $HOME/toolchains/clang/bin/ | head -20
          
          # Export paths
          echo "CLANG_PATH=$HOME/toolchains/clang" >> $GITHUB_ENV
          echo "$HOME/toolchains/clang/bin" >> $GITHUB_PATH
      
      - name: Clone SUSFS kernel patches
        if: ${{ inputs.enable_susfs != false }}
        run: |
          echo "=== Cloning SUSFS kernel patches ==="
          
          # Determine SUSFS branch based on kernel version
          case "${{ env.KERNEL_MAJOR }}" in
            "4.14") SUSFS_BRANCH="kernel-4.14" ;;
            "4.19") SUSFS_BRANCH="kernel-4.19" ;;
            "5.4")  SUSFS_BRANCH="kernel-5.4" ;;
            *)      SUSFS_BRANCH="kernel-4.19" ;;  # Default fallback
          esac
          
          echo "Using SUSFS branch: $SUSFS_BRANCH for kernel ${{ env.KERNEL_MAJOR }}"
          
          # Clone SUSFS repository
          git clone --depth=1 --branch $SUSFS_BRANCH https://gitlab.com/simonpunk/susfs4ksu.git susfs || {
            echo "Failed to clone SUSFS branch $SUSFS_BRANCH, trying master..."
            git clone --depth=1 https://gitlab.com/simonpunk/susfs4ksu.git susfs
          }
          
          # List available patches
          echo "=== SUSFS kernel patches available ==="
          if [ -d "susfs/kernel_patches" ]; then
            ls -la susfs/kernel_patches/
          else
            echo "No kernel_patches directory found"
            find susfs -name "*.patch" -o -name "*.diff" 2>/dev/null
          fi
          
          echo "SUSFS_BRANCH=$SUSFS_BRANCH" >> $GITHUB_ENV
      
      - name: Apply SUSFS kernel patches
        if: ${{ inputs.enable_susfs != false }}
        run: |
          echo "=== Applying SUSFS kernel patches ==="
          
          PATCH_DIR="susfs/kernel_patches"
          
          if [ ! -d "$PATCH_DIR" ]; then
            echo "WARNING: No kernel_patches directory found, SUSFS may not work correctly"
            echo "Continuing without SUSFS kernel patches..."
            exit 0
          fi
          
          # Function to apply patch with error handling
          apply_patch() {
            local patch_file="$1"
            local patch_name=$(basename "$patch_file")
            
            echo "Applying patch: $patch_name"
            
            # Try to apply with git apply first
            if git apply --check "$patch_file" 2>/dev/null; then
              git apply "$patch_file"
              echo "  ✓ Applied with git apply"
              return 0
            fi
            
            # Try patch command with different options
            if patch -p1 --dry-run < "$patch_file" 2>/dev/null; then
              patch -p1 < "$patch_file"
              echo "  ✓ Applied with patch -p1"
              return 0
            fi
            
            if patch -p0 --dry-run < "$patch_file" 2>/dev/null; then
              patch -p0 < "$patch_file"
              echo "  ✓ Applied with patch -p0"
              return 0
            fi
            
            echo "  ✗ Could not apply patch: $patch_name"
            echo "  This may require manual patching for your kernel"
            return 1
          }
          
          # Apply all patches in the directory
          PATCHES_APPLIED=0
          PATCHES_FAILED=0
          
          for patch_file in "$PATCH_DIR"/*.patch "$PATCH_DIR"/*.diff; do
            if [ -f "$patch_file" ]; then
              if apply_patch "$patch_file"; then
                PATCHES_APPLIED=$((PATCHES_APPLIED + 1))
              else
                PATCHES_FAILED=$((PATCHES_FAILED + 1))
              fi
            fi
          done
          
          echo ""
          echo "=== SUSFS Patch Summary ==="
          echo "Patches applied: $PATCHES_APPLIED"
          echo "Patches failed: $PATCHES_FAILED"
          
          if [ $PATCHES_FAILED -gt 0 ]; then
            echo "WARNING: Some patches failed to apply. SUSFS may not work correctly."
            echo "You may need to manually patch the kernel for your specific version."
          fi
          
          # Create include/linux/susfs.h if not created by patches
          if [ ! -f "include/linux/susfs.h" ] && [ -f "susfs/include/linux/susfs.h" ]; then
            cp susfs/include/linux/susfs.h include/linux/susfs.h
            echo "Copied susfs.h to include/linux/"
          fi
          
          echo "=== SUSFS patching complete ==="
      
      - name: Clone and integrate ReSukiSU
        run: |
          echo "=== Integrating ReSukiSU ==="
          
          # Clone ReSukiSU
          if [ -n "${{ inputs.ksu_version }}" ]; then
            echo "Cloning ReSukiSU at version: ${{ inputs.ksu_version }}"
            git clone --depth=1 --branch ${{ inputs.ksu_version }} https://github.com/ReSukiSU/ReSukiSU KernelSU || \
            git clone --depth=1 https://github.com/ReSukiSU/ReSukiSU KernelSU && cd KernelSU && git checkout ${{ inputs.ksu_version }}
          else
            echo "Cloning latest ReSukiSU..."
            git clone --depth=1 https://github.com/ReSukiSU/ReSukiSU KernelSU
          fi
          
          cd KernelSU
          echo "ReSukiSU version info:"
          git log -1 --oneline
          KSU_COMMIT=$(git rev-parse --short HEAD)
          echo "KSU_COMMIT=$KSU_COMMIT" >> $GITHUB_ENV
          cd ..
          
          # Determine drivers directory
          if [ -d "common/drivers" ]; then
            DRIVER_DIR="common/drivers"
          elif [ -d "drivers" ]; then
            DRIVER_DIR="drivers"
          else
            echo "ERROR: drivers/ directory not found"
            exit 1
          fi
          
          echo "Using drivers directory: $DRIVER_DIR"
          
          # Create symlink to KernelSU kernel module
          KERNELSU_KERNEL_PATH="$(pwd)/KernelSU/kernel"
          rm -f "$DRIVER_DIR/kernelsu"
          ln -sf "$KERNELSU_KERNEL_PATH" "$DRIVER_DIR/kernelsu"
          echo "Created symlink: $DRIVER_DIR/kernelsu -> $KERNELSU_KERNEL_PATH"
          
          # Verify symlink
          ls -la "$DRIVER_DIR/kernelsu"
          
          # Add kernelsu to drivers Makefile
          if ! grep -q "kernelsu" "$DRIVER_DIR/Makefile"; then
            echo "" >> "$DRIVER_DIR/Makefile"
            echo 'obj-$(CONFIG_KSU) += kernelsu/' >> "$DRIVER_DIR/Makefile"
            echo "Added kernelsu to $DRIVER_DIR/Makefile"
          fi
          
          # Add kernelsu Kconfig to drivers Kconfig
          if ! grep -q "kernelsu/Kconfig" "$DRIVER_DIR/Kconfig"; then
            echo "Adding kernelsu Kconfig to $DRIVER_DIR/Kconfig..."
            
            # Backup original
            cp "$DRIVER_DIR/Kconfig" "$DRIVER_DIR/Kconfig.backup"
            
            # Insert before endmenu or append
            if grep -q "^endmenu" "$DRIVER_DIR/Kconfig"; then
              sed -i '/^endmenu/i source "drivers/kernelsu/Kconfig"' "$DRIVER_DIR/Kconfig"
            else
              echo '' >> "$DRIVER_DIR/Kconfig"
              echo 'source "drivers/kernelsu/Kconfig"' >> "$DRIVER_DIR/Kconfig"
            fi
            
            if grep -q "kernelsu/Kconfig" "$DRIVER_DIR/Kconfig"; then
              echo "Successfully added kernelsu Kconfig"
            else
              echo "ERROR: Failed to add kernelsu Kconfig"
              cp "$DRIVER_DIR/Kconfig.backup" "$DRIVER_DIR/Kconfig"
              exit 1
            fi
          fi
          
          echo "=== ReSukiSU integration complete ==="
      
      - name: Configure kernel
        run: |
          echo "=== Configuring kernel ==="
          
          # Clean previous build
          make mrproper || true
          
          # Create output directory
          mkdir -p out
          
          # Apply base defconfig
          echo "Applying defconfig: ${{ env.DEVICE_CONFIG }}"
          make O=out ARCH=arm64 ${{ env.DEVICE_CONFIG }} \
            CC=$HOME/toolchains/clang/bin/clang \
            CLANG_TRIPLE=aarch64-linux-gnu- \
            HOSTCC=$HOME/toolchains/clang/bin/clang
          
          if [ ! -f "out/.config" ]; then
            echo "ERROR: Failed to create .config"
            exit 1
          fi
          
          echo "=== Enabling KSU and required options ==="
          
          # Enable core KSU option
          ./scripts/config --file out/.config --enable KSU
          
          # Enable required kernel options
          ./scripts/config --file out/.config --enable KPROBES
          ./scripts/config --file out/.config --enable KALLSYMS
          ./scripts/config --file out/.config --enable KALLSYMS_ALL
          
          # THREAD_INFO_IN_TASK is required for SUSFS
          if grep -q "CONFIG_THREAD_INFO_IN_TASK" out/.config; then
            ./scripts/config --file out/.config --enable THREAD_INFO_IN_TASK
            echo "Enabled THREAD_INFO_IN_TASK"
          fi
          
          # Enable SUSFS options if requested
          if [ "${{ inputs.enable_susfs }}" != "false" ]; then
            echo "Enabling SUSFS options..."
            ./scripts/config --file out/.config --enable KSU_SUSFS
            ./scripts/config --file out/.config --enable KSU_SUSFS_SUS_MOUNT
            ./scripts/config --file out/.config --enable KSU_SUSFS_SPOOF_UNAME
            ./scripts/config --file out/.config --enable KSU_SUSFS_ENABLE_LOG
            ./scripts/config --file out/.config --enable KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS
            ./scripts/config --file out/.config --enable KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG
            ./scripts/config --file out/.config --enable KSU_SUSFS_SUS_MAP
          fi
          
          # Enable debug options if requested
          if [ "${{ inputs.enable_debug }}" = "true" ]; then
            ./scripts/config --file out/.config --enable KSU_DEBUG
          fi
          
          # Update config - resolve dependencies
          echo "Running olddefconfig..."
          make O=out ARCH=arm64 olddefconfig \
            CC=$HOME/toolchains/clang/bin/clang \
            CLANG_TRIPLE=aarch64-linux-gnu- \
            HOSTCC=$HOME/toolchains/clang/bin/clang
          
          # Verify configuration
          echo "=== Verifying kernel config ==="
          grep -E "CONFIG_KSU|CONFIG_KPROBES|CONFIG_KALLSYMS|CONFIG_THREAD_INFO_IN_TASK" out/.config || true
          
          echo "Kernel configuration complete"
      
      - name: Build kernel
        run: |
          echo "=== Building kernel ==="
          
          JOBS=$(nproc --all)
          echo "Building with $JOBS jobs"
          
          # Build kernel
          make -j$JOBS O=out ARCH=arm64 \
            CC=$HOME/toolchains/clang/bin/clang \
            CLANG_TRIPLE=aarch64-linux-gnu- \
            HOSTCC=$HOME/toolchains/clang/bin/clang \
            HOSTLD=$HOME/toolchains/clang/bin/ld.lld \
            2>&1 | tee build.log
          
          BUILD_EXIT_CODE=${PIPESTATUS[0]}
          echo "Build exit code: $BUILD_EXIT_CODE"
          
          # Check if build was successful
          if [ ! -f "out/arch/arm64/boot/Image" ]; then
            echo "ERROR: Kernel Image not found!"
            echo "=== Last 150 lines of build log ==="
            tail -150 build.log
            exit 1
          fi
          
          # Copy kernel images
          if [ -f "out/arch/arm64/boot/Image.gz-dtb" ]; then
            echo "Found Image.gz-dtb"
            cp out/arch/arm64/boot/Image.gz-dtb ${{ env.KERNEL_NAME }}-Image.gz-dtb
          elif [ -f "out/arch/arm64/boot/Image.gz" ]; then
            echo "Found Image.gz"
            cp out/arch/arm64/boot/Image.gz ${{ env.KERNEL_NAME }}-Image.gz
          fi
          
          cp out/arch/arm64/boot/Image ${{ env.KERNEL_NAME }}-Image
          
          echo "=== Build artifacts ==="
          ls -la ${{ env.KERNEL_NAME }}-*
          
          echo "Build completed successfully"
      
      - name: Package kernel
        run: |
          echo "=== Packaging kernel ==="
          
          mkdir -p output
          
          # Copy kernel images
          cp ${{ env.KERNEL_NAME }}-Image output/
          [ -f "${{ env.KERNEL_NAME }}-Image.gz" ] && cp ${{ env.KERNEL_NAME }}-Image.gz output/
          [ -f "${{ env.KERNEL_NAME }}-Image.gz-dtb" ] && cp ${{ env.KERNEL_NAME }}-Image.gz-dtb output/
          
          # Copy modules if available
          [ -d "out/modules" ] && cp -r out/modules output/
          
          # Generate version info
          cat > output/version.txt << EOF
          Kernel: ${{ env.KERNEL_NAME }}
          Version: ${{ env.KERNEL_FULL_VERSION }}
          Device Config: ${{ env.DEVICE_CONFIG }}
          Build Date: ${{ env.BUILD_DATE }}
          Build Type: ${{ env.BUILD_TYPE }}
          ReSukiSU Commit: ${{ env.KSU_COMMIT }}
          SUSFS Branch: ${{ env.SUSFS_BRANCH }}
          SUSFS Enabled: ${{ inputs.enable_susfs || 'true' }}
          GitHub Run: ${{ github.run_number }}
          Source Commit: ${{ github.sha }}
          EOF
          
          # Create zip
          cd output
          zip -r ../${{ env.KERNEL_NAME }}-${{ env.BUILD_DATE }}.zip *
          cd ..
          
          # Calculate checksums
          sha256sum ${{ env.KERNEL_NAME }}-${{ env.BUILD_DATE }}.zip > ${{ env.KERNEL_NAME }}-${{ env.BUILD_DATE }}.zip.sha256
          
          echo "Package created: ${{ env.KERNEL_NAME }}-${{ env.BUILD_DATE }}.zip"
          ls -la ${{ env.KERNEL_NAME }}-*
      
      - name: Upload kernel artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.KERNEL_NAME }}-${{ env.BUILD_DATE }}
          path: |
            output/
            ${{ env.KERNEL_NAME }}-${{ env.BUILD_DATE }}.zip
            ${{ env.KERNEL_NAME }}-${{ env.BUILD_DATE }}.zip.sha256
          retention-days: 30
      
      - name: Upload build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-log-${{ env.BUILD_DATE }}
          path: build.log
          retention-days: 7

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-22.04
    if: startsWith(github.ref, 'refs/tags/')
    
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
      
      - name: Create release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            artifacts/**/*.zip
            artifacts/**/*.sha256
          generate_release_notes: true
          draft: false
          prerelease: ${{ contains(github.ref, '-rc') || contains(github.ref, '-beta') || contains(github.ref, '-alpha') }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
